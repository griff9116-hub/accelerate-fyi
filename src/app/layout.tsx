import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: { default: "Accelerate.fyi — UK Accelerator & VC Directory", template: "%s | Accelerate.fyi" },
  description: "Find the right UK accelerator, venture studio, or investor for your startup. Filter by sector, stage, location, SEIS/EIS, equity terms and more.",
  openGraph: {
    title: "Accelerate.fyi — UK Accelerator & VC Directory",
    description: "Find the right UK accelerator, venture studio, or investor for your startup.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
      <body className="flex min-h-screen flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
